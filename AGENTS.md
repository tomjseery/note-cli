# note

A portable C++ command-line notes application built with CMake. Apply the
installed `cpp-standards` plugin everywhere and layer `gpp-standards` on Linux;
neither depends on this repository's checkout path.

Read `PROBLEM.md` for the current cross-toolchain build issue and `PROGRESS.md`
for the project-specific work state.
